final class AppDurations {
  const AppDurations._();

  static const Duration micro = Duration(milliseconds: 90);
  static const Duration short = Duration(milliseconds: 180);
  static const Duration medium = Duration(milliseconds: 360);
  static const Duration long = Duration(milliseconds: 620);
  static const Duration cinematic = Duration(milliseconds: 1180);
  static const Duration epic = Duration(milliseconds: 2200);

  static const Duration menuActionHold = Duration(milliseconds: 260);
  static const Duration buttonActivation = Duration(milliseconds: 230);
  static const Duration loadingScreen = Duration(milliseconds: 3600);
  static const Duration splashScreen = Duration(milliseconds: 2350);

  static const Duration routeTitle = Duration(milliseconds: 820);
  static const Duration routeHome = Duration(milliseconds: 860);
  static const Duration routeSettings = Duration(milliseconds: 620);
  static const Duration routeLoading = Duration(milliseconds: 620);
  static const Duration routeGame = Duration(milliseconds: 680);
  static const Duration routeReverse = Duration(milliseconds: 360);

  static const Duration titleBackground = Duration(milliseconds: 2200);
  static const Duration titleLogo = Duration(milliseconds: 5600);
  static const Duration titlePrompt = Duration(milliseconds: 900);
  static const Duration titlePress = Duration(milliseconds: 520);
  static const Duration titleMotesLoop = Duration(seconds: 9);
  static const Duration titleLogoEntranceDelay = Duration(seconds: 2);
  static const Duration titlePromptDelay = Duration(seconds: 5);
  static const Duration titleRouteHandoff = Duration(milliseconds: 260);

  static const Duration dialogTransition = Duration(milliseconds: 460);
  static const Duration dialogTitleGlow = Duration(milliseconds: 620);
  static const Duration bannerTextFade = Duration(milliseconds: 620);
  static const Duration bannerDeathBand = Duration(milliseconds: 780);

  static const Duration boardMarkReveal = Duration(milliseconds: 420);
  static const Duration boardParticleRing = Duration(milliseconds: 900);
  static const Duration boardWinBeam = Duration(milliseconds: 1080);
  static const Duration boardDrawFog = Duration(milliseconds: 1400);
  static const Duration boardDrawShake = Duration(milliseconds: 360);

  static const Duration selectionGlow = Duration(milliseconds: 950);

  static const Duration sceneEmbersLoop = Duration(seconds: 14);
  static const Duration graceBreath = Duration(seconds: 4);
  static const Duration fogDrift = Duration(seconds: 18);
}
